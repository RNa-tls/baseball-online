# DUGOUT DUEL — 아키텍처 문서

2인 온라인 멀티플레이 야구 게임. 서버 권위(server-authoritative) 구조.

## 파일 구조

```
server.js             # Express 정적 서빙 + ws 웹소켓 + 방 관리 (네트워크 계층만)
game/constants.js     # 필드 좌표, 라인업/능력치, 구종 파라미터 (순수 데이터)
game/engine.js        # 게임 로직 전체 (상태머신 + 물리 시뮬레이션). 네트워크 의존 없음
public/index.html     # 클라이언트 전부 (Canvas 렌더 + 입력 + WebAudio). 외부 라이브러리 없음
test/engine.test.js   # 헤드리스 엔진 테스트 (node --test)
tools/balance-sim.js  # 투타 밸런스 측정 시뮬레이터 (사람 모사 봇 풀게임)
```

엔진은 `update(dtMs)` + `handleXxx()` 입력 메서드 + 이벤트 큐(`drainEvents()`)만 노출한다.
시간은 전부 내부 누적 카운터로 관리하므로 node에서 임의 속도로 스텝하며 테스트 가능.
RNG는 생성자 주입(`new GameEngine({rng})`)으로 테스트 시 결정적 실행 가능.

## 서버 상태머신 (엔진 phase)

```
LOBBY → PITCHING → PITCH_LIVE → (LIVE | PITCHING) → … → MID_INNING → PITCHING → … → GAME_OVER
                         ↑________________________________|
어느 단계에서든 플레이어 이탈 시 PAUSED(이전 phase 저장) → 30초 내 재접속 시 복귀, 아니면 몰수승
```

- **PITCHING**: 수비측의 `pitch` 입력 대기. 이 단계에서만 불펜 교체(`swap`) 허용.
- **PITCH_LIVE**: 투구 비행 중. 공격측 `swing` 대기. `flightMs + 400ms`(레이턴시 유예) 내
  스윙 없으면 플레이트 위치로 볼/스트라이크 자동 판정.
- **LIVE**: 인플레이. 20tps 시뮬레이션(타구/야수/주자/송구). 스냅샷 브로드캐스트.
- **MID_INNING**: 공수교대 연출 3초.
- **GAME_OVER**: `newGame`으로 리매치 가능.

## 메시지 프로토콜 (JSON, `t` 필드가 타입)

### 클라 → 서버
| t | payload | 설명 |
|---|---|---|
| `create` | — | 방 생성 |
| `join` | `code` | 코드로 참가 |
| `rejoin` | `code, token` | 재접속 |
| `ping` | `ts` | 핑 측정 (2초 주기) |
| `pitch` | `kind, aim:{x,y}, power` | 구종/코스/파워게이지(0..1, 1=정점) |
| `swap` | — | 불펜 교체 (팀당 1회) |
| `swing` | `aim:{x,y}, dt, bunt` | dt=클라가 투구 시작 렌더 후 경과 ms |
| `move` | `dir:{x,y}` | 수비 야수 이동 (입력 스트림: 방향 바뀔 때 전송) |
| `throw` | `base` (1..4, 4=홈) | 송구 |
| `run` | `cmd:'go'\|'stop'` | 전체 진루 / 정지·귀루 |
| `newGame` | — | 리매치 |

### 서버 → 클라
| t | 설명 |
|---|---|
| `created` / `joined` | `code, token` (token은 재접속용) |
| `start` | `you:'home'\|'away'`, `config`(라인업/필드 지오메트리 — 클라는 데이터 기반 렌더) |
| `state` | 풀 상태: 스코어보드, 이닝, 카운트, 베이스, 타자/투수, phase. 타석마다 + 재접속 시 |
| `pitchStart` | `kind, plate:{x,y}, breakX/Y, flightMs, ...` — 클라가 동일 궤적 애니메이션 |
| `pitchResult` | `ball/strike/foul/whiff/inPlay` + 카운트 |
| `snap` | LIVE 중 20tps: 공(x,y,z)/야수 9명/주자/조작 야수 인덱스/볼 소유자 |
| `playResult` | 플레이 요약, 아웃/점수/스플래시(`HOME RUN!` 등) |
| `splash` | 큰 텍스트 연출 트리거 |
| `oppLeft` / `oppBack` | 상대 이탈/복귀 (`deadline` 포함) |
| `gameOver` | 승자, 최종 스코어 |
| `pong` | 핑 응답 |

## 동기화 설계

- 서버 룸당 50ms(20tps) 고정 틱으로 `engine.update(50)` 호출. LIVE phase에서만 스냅샷 송신.
- 클라이언트는 스냅샷 2개 사이를 보간하며 약 120ms 지연 렌더.
- **투구↔타격 타이밍 공정성**: 스윙 판정은 "클라이언트가 투구를 화면에 그리기 시작한 시점부터의
  경과시간(dt)"을 클라가 측정해 보내고, 서버는 `dt vs flightMs`로 타이밍을 판정한다.
  → 양쪽 핑이 달라도 타이밍 게임이 공정함. (친구 간 게임이므로 dt 변조 방지는 범위 외,
  서버는 dt가 `[0, windup+flightMs+400]` 범위인지만 검증)

## 투타 밸런스 (측정 기반 — v1.2 "가시성 우선" 재설계)

### 경위
- v1.0 실플레이: "2이닝 안타 1, 루킹삼진 4" → v1.1에서 구속 하향/판정 완화로 봇 컨택 81% 달성.
- 그러나 v1.1 실플레이에서도 **컨택 0%** (헛스윙 삼진 연발). 원인: 봇은 내부 시계로
  "지금 클릭"을 정확히 알지만, **사람에겐 그 시점을 알려주는 시각 신호가 없었다**.
  타이밍 정보가 게임 화면에 존재하지 않으면 판정을 아무리 완화해도 사람은 못 친다.

### v1.2 핵심: 타이밍의 가시화 (판정 완화보다 우선)
1. **수축 링** — 투구 시작과 함께 도달점 주위의 흰 링이 줄어들어,
   **노란 도달점 링과 겹치는 순간 = 정타 타이밍**. 시선 분산 없이 한 지점만 보면 됨.
2. **타이밍 바 게이지** — 존 아래에 진행 바늘 + 커트(노랑)/정타(초록) 구간 + 완벽 중앙선.
3. **도달점 링을 비행 12%부터** 표시 (기존 35%는 마우스 이동 시간 부족), 크기/대비 강화.
4. **정타 존 진입 신호** — 링 초록 플래시 + 1320Hz 사운드 큐.
5. **스윙 교정 피드백** — 헛스윙/파울 시 "NNms 빨랐음/늦었음", 조준 미스면 별도 안내.
6. **첫 3스윙 연습 가이드** 텍스트.
7. **레이턴시 유예 400ms로 확대** — 핑 100ms + 늦은 스윙 꼬리가 250ms 유예를 넘으면
   스윙이 통째로 무시되던 문제 방지.

판정도 소폭 재조정: 클린 컨택 윈도우는 살짝 좁히고(hitWin 0.47+con×0.05,
timingWin 70+con×7) **파울 구간을 크게** (dN≤1.5, tN≤1.4까지 커트).
타구속도 `61 + q^0.8×(58+pow×12.5)`.

### 검증 — 비관적 봇 (반응 지연 250~350ms, 시각 신호 없으면 타이밍 추정 불가 가정)
`node tools/balance-sim.js [games] [seed] [--noaids]`

| 지표 (9이닝) | 목표 | 보조 ON (70경기) | 보조 OFF (v1.1 모사) |
|---|---|---|---|
| 총 득점/경기 | 7~12 | 8.7 ~ 11.2 | 2.3 |
| 팀 타율 | 0.230~0.280 | 0.246 ~ 0.269 | 0.079 |
| 삼진/팀 | 6~10 | 5.5 ~ 6.0 | 21.4 |
| 볼넷/팀 | 2~5 | 2.5 ~ 3.5 | 3.6 |
| 홈런/경기 | 1~3 | 1.3 ~ 1.9 | 0.2 |
| 컨택률(스윙 중) | ≥50% | **88~89%** | 53% |

보조 OFF 측정이 실측 증상(헛스윙 삼진 연발, 득점 거의 0)을 재현함 → 가시성이
결정 변수였음을 확인. 삼진 5.5~6.0은 하한 경계지만, 실제 사람은 봇보다 노이즈가
크므로 실플레이 삼진은 이보다 높게 나온다 — "타격이 너무 어려움" 쪽(상한 초과)을
피하는 방향의 오차라 채택.

### v1.1에서 유지된 레버
- **구속 전반 하향** — flightMs 560~760 (구종 간 속도차 비율 유지 → 수싸움 보존).
- **약한 컨택(q<0.18)은 파울팁**, **2스트라이크 커트 보정** ×1.15.
- **뜬공 수비**: 낙하 예측점 X 마커 + AI 1명만 낙하점 추격 (전원 수렴 시 BABIP 붕괴).
  v1.1 진단: 타율 폭등의 주범은 타격이 아니라 뜬공 수비 실패(플라이 안타율 0.795)였음.
- **펜스 단축** (센터 388 / 폴 ~318), 야수선택은 안타 아님, 타임아웃 시 주자 3루 정리.

## 게임플레이 판정 요약

- **투구**: 구종별 (비행시간, 무브먼트, 제구 보정). 파워 게이지 정점에서 구속↑·제구 정확,
  벗어나면 제구 오차 증가. 스태미나 30 미만 시 구속 -7%, 제구 오차 ×1.6.
- **스윙**: 위치오차/타이밍오차를 컨택 윈도우로 정규화(dN, tN) →
  클린 컨택은 q=(1-dN)(1-tN)로 타구, 가장자리는 파울, 그 밖은 헛스윙 (밸런스 섹션 참고).
  타구는 발사각(스윙 높낮이 오차) + 스프레이각(타이밍: 빠르면 당겨침)
  + 타구속도(파워 스탯 × q)로 결정 → 땅볼/라이너/플라이/홈런이 물리로 자연 발생.
- **타구 물리**: 탑뷰 2D + 높이 z. 중력/간이 드래그/바운드/구름 마찰. 펜스(좌우 ~318ft~중앙 388ft,
  높이 10ft) 넘으면 홈런, 맞으면 튕김. 파울라인 ±45°.
- **수비**: 조작 야수 = 볼 소유자, 없으면 공에 가장 가까운 야수(0.5초마다 재평가).
  나머지는 AI가 베이스 커버. 송구 오차 = 거리 × 수비스탯 보정 × 랜덤, 크게 빗나가면 E(실책).
- **주루**: 땅볼 시 포스 주자 자동 스타트. 플라이 포구 시 자동 귀루(리터치) 후 `go`로 태그업
  → 희생플라이 자연 구현. 포스아웃/태그아웃은 공 도착 vs 주자 도착 비교.
- **플레이 종료**: 모든 주자가 베이스 위 정지 + 야수가 공 소유 상태 0.8초 지속, 또는 25초 타임아웃.

## 설계 결정 근거 (스펙 모호 부분)

1. **도루 생략** — 스펙의 "완성도 애매하면 기능 축소" 지침에 따름(스펙 예시 그대로).
   투구↔타격 루프와 주루 판정 퀄리티에 집중.
2. **주자별 개별 지시 → 전체 지시로 단순화** — `go`(우클릭)/`stop`(Space) 2버튼.
   포스 상황 자동 처리와 결합하면 개별 지시 없이도 전술적 선택(태그업, 추가진루)이 성립.
3. **타이밍 판정의 클라 신뢰(dt)** — 친구 간 사설 게임이라 공정성(레이턴시 보정)이
   치트 방지보다 우선.
4. **선수 이름/스탯 등 config를 start 메시지로 전송** — 클라/서버 상수 중복 정의로 인한
   드리프트 방지 (클라는 단일 HTML이라 모듈 공유 불가).
5. **양 팀 동일 라인업** — 스펙 명시. 1번 주력형(주력9), 4번 파워형(파워9), 9번 투수(타격 최약).
6. **연장 12회 종료 후 무승부** — 스펙 명시. 끝내기(말 공격 리드 시 즉시 종료) 구현.
7. **에러(E) 정의** — 송구가 목표 베이스에서 6ft 이상 벗어난 "악송구"만 카운트.
   (포구 실수 등은 판정 모호성 때문에 제외)
8. **픽셀 아트** — 640×360 내부 해상도 백버퍼에 그린 뒤 `imageSmoothingEnabled=false`로
   정수 배율 확대. 모든 스프라이트는 코드로 그린 사각형 조합(외부 에셋 금지 준수).

## 테스트

```
npm test        # node --test test/  — 엔진 헤드리스 테스트
```

볼넷/삼진/파울 규칙, 홈런 득점, 3아웃 교대, 9이닝 종료, 연장/무승부, 끝내기,
땅볼 → 1루 송구 포스아웃, 희생플라이 시나리오를 스크립트 입력으로 검증.

## 배포 (Railway)

- `package.json`의 `start: node server.js`, 포트는 `process.env.PORT || 3000`.
- 빌드 단계 없음. WebSocket은 같은 HTTP 서버에 업그레이드로 붙음(별도 포트 불필요).
